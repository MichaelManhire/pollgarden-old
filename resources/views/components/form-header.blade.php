<div class="text-center">
    <img class="mx-auto" src="{{ asset('images/logo.svg') }}" alt="Poll Garden" height="64" width="64" loading="lazy">
    <h1 class="mt-4 text-3xl font-extrabold leading-9">{{ $title }}</h1>
    <p class="mt-2 text-sm leading-5 text-gray-600">{{ $slot }}</p>
</div>
